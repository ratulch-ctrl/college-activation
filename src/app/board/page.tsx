import { ComingSoon } from '@/components/ComingSoon';

export default function BoardPage() {
  return (
    <ComingSoon
      title="Stream Board"
      note="A board of every college's streams grouped by stage lands in an upcoming pull request."
    />
  );
}
