import { ComingSoon } from '@/components/ComingSoon';

export default function NewInteractionPage() {
  return (
    <ComingSoon
      title="Log Interaction"
      note="The interaction logger — with stage-driven forms that advance a stream's stage — lands in an upcoming pull request."
    />
  );
}
